using UnityEngine;
using System.Collections;

public class TestScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void FixedUpdate () {
		//Physics2D.gravity = new Vector2(15*Mathf.Sin(Time.time+0.75f),15*Mathf.Sin(Time.time+0.3f));
		//Physics2D.gravity = -Vector2.up*10;
	}
}
